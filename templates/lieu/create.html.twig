{% extends 'base.html.twig' %}

{% block main %}
   <h1>Créer un lieu de sortie</h1>
    <br>

    <div class="content container ">
        <div class="row justify-content-md-center">

    {{ form_start(lieuForm) }}
        <div class="form-group row">
            <div class="col-md-4">
                {{ form_label(lieuForm.ville) }}
            </div>
            <div class="col-md-8">
                {{ form_widget(lieuForm.ville) }}
                {{ form_errors(lieuForm.ville) }}
            </div>
        </div>
        <div class="form-group row">
            <div class="col-md-4">
                {{ form_label(lieuForm.nom) }}
            </div>
            <div class="col-md-8">
                {{ form_widget(lieuForm.nom) }}
                {{ form_errors(lieuForm.nom) }}
            </div>
        </div>
        <div class="form-group row">
            <div class="col-md-4">
                {{ form_label(lieuForm.rue) }}
            </div>
            <div class="col-md-8">
                {{ form_widget(lieuForm.rue) }}
                {{ form_errors(lieuForm.rue) }}
            </div>
        </div>
        <div class="form-group row">
            <div class="col-md-4">
                {{ form_label(lieuForm.latitude)}}
            </div>
            <div class="col-md-8">
                {{ form_widget(lieuForm.latitude) }}
                {{ form_errors(lieuForm.latitude) }}
            </div>
        </div>
        <div class="form-group row">
            <div class="col-md-4">
                {{ form_label(lieuForm.longitude) }}
            </div>
            <div class="col-md-8">
                {{ form_widget(lieuForm.longitude) }}
                {{ form_errors(lieuForm.longitude) }}
            </div>
        </div>



        <br>
    <input type="submit" class="btn btn-primary" value="Enregistrer" href="/sortie/create.html.twig">
    <input type="submit" class="btn btn-primary" value="Annuler">
    {{ form_end(lieuForm) }}
        </div>
    </div>

{% endblock %}