{% extends 'base.html.twig' %}

{% block title %}Gérer les sites{% endblock %}

{% block body %}
    <div class="Page container">
        <h1 class="text-center display-4 lead"><b>{{ page_name }}</b></h1>
        {{ form_start(form_site) }}
        {{ form_errors(form_site) }}
        {{ form_end(form_site) }}
        <a href="{{ path("site") }}"class="btn btn-light mr-1"> Retour </a>
    </div>
{% endblock %}